##############################################################################
#
# File:	pyproject.toml
#
# Function:
#	Provides project settings for use by uv.
#
# Copyright notice and license:
#	See LICENSE.md in this directory.
#
# Author:
#	Terry Moore
#	Portions generated by m365.cloud.microsoft.
#
##############################################################################

[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "annotate-film-scans"
version = "3.0.0b1"	# uv won't accepet 3.0.0-pre1, makes it 3.0.0rc1; so use a for Alpha, b for Beta, or rc.
description = "Batch film-scan annotator"
readme = "README.md"
requires-python = ">=3.13"
license = { file = "LICENSE.md" }
authors = [{ name = "Terry Moore", email = "terrillmoore@yahoo.com" }]
keywords = ["exif", "film", "photography", "tagging"]
dependencies = [
	"jsons >= 1.6.3,== 1.*"
]

[project.urls]
github = "https://github.com/terrillmoore/annotate_film_scans"

[project.scripts]
annotate-film-scans = "annotate_film_scans.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["annotate_film_scans"]

### end of file ###
